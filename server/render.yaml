services:
  - type: web
    name: cropmate-ai-backend
    runtime: python
    buildCommand: |
      git lfs install --force
      git lfs pull
      pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT app:app
    envVars:
      - key: FLASK_ENV
        value: production
